---
- name: Full L3 Configuration for MikroTik hEX Lite
  hosts: mikrotiks
  gather_facts: false
  connection: local

  module_defaults:
    community.routeros.api:
      hostname: "{{ ansible_host }}"
      username: "{{ ansible_user }}"
      password: "{{ ansible_password }}"

  roles:
    - role: common
    - role: router
      when: "'microtiks' in group_names"
